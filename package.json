{
  "name": "daemonchat",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/adapters/*",
    "packages/platform/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "bun-types": "^1.3.8",
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  }
}
