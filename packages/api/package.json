{
  "name": "@daemon/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"lint not configured\""
  },
  "dependencies": {
    "@daemon/domain": "workspace:*",
    "@trpc/server": "^11.0.0",
    "zod": "^3.24.1"
  }
}
